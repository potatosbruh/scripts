function easyTP(placeCFrame)
	local player = game.Players.LocalPlayer;
	if player.Character then
		player.Character.HumanoidRootPart.CFrame = placeCFrame
	end
end

function oilCup()
	easyTP(game:GetService("Workspace").oil.Part.CFrame)
end

function bloodCup()
	easyTP(game:GetService("Workspace").blood.Handle.CFrame)
end

function mystCup()
	easyTP(game:GetService("Workspace")["mysterious cup"].Handle.CFrame)
end

function tpForge()
    easyTP(game:GetService("Workspace").Forging.anvil.Part.CFrame)
end

function tpRiver()
	easyTP(game:GetService("Workspace")["Red dimension"].HeartyFall.Red2.CFrame)
end

function tpUpgrade()
	easyTP(game:GetService("Workspace").upgrader.blood.a.CFrame)
end

function tradeCenter()
	easyTP(game:GetService("Workspace")["Reworked_Trade_Center"].Teleport.CFrame)
end

function darkOrb()
	easyTP(game:GetService("Workspace").orb.Part.CFrame)
end

function nightEssence()
	easyTP(game:GetService("Workspace")["Night's anvil plate"].Part.CFrame)
end

function sorrowCup()
	easyTP(game:GetService("Workspace")["sorrow cup"].Handle.CFrame)
end

function nightsEssence()
	easyTP(game:GetService("Workspace")["nights essence"].Handle.CFrame)
end

function relicFarm(relicSelected)
	easyTP(game:GetService("Workspace")[relicSelected].Handle.CFrame)
end

function uselessHole()
	easyTP(game:GetService("Workspace")["The hole"].Part.CFrame)
end

function trader()
	easyTP(game:GetService("Workspace")["Red Tree"].RedTreePortal.CFrame)
end

-- init
local library = loadstring(game:HttpGet("https://raw.githubusercontent.com/GreenDeno/Venyx-UI-Library/main/source.lua"))()
local venyx = library.new("World of Trollge", 5013109572)

-- themes
local themes = {
Background = Color3.fromRGB(24, 24, 24),
Glow = Color3.fromRGB(0, 0, 0),
Accent = Color3.fromRGB(10, 10, 10),
LightContrast = Color3.fromRGB(20, 20, 20),
DarkContrast = Color3.fromRGB(14, 14, 14),  
TextColor = Color3.fromRGB(255, 255, 255)
}

-- first page
local page = venyx:addPage("Autos", 5012544693)
local sec1 = page:addSection("Autos")

sec1:addToggle("oil", nil, function(value)
	while value == true do
		oilCup()
		wait(.5)
	end
end)

sec1:addToggle("blood", nil, function(value)
	while value == true do
		bloodCup()
		wait(.5)
	end
end)

sec1:addToggle("mysterious cup", nil, function(value)
	while value == true do
		mystCup()
		wait(.5)
	end
end)

sec1:addToggle("sorrow cup", nil, function(value)
	while value == true do
		sorrowCup()
		wait(.5)
	end
end)

sec1:addToggle("nights essence", nil, function(value)
	while value == true do
		nightsEssence()
		wait(.5)
	end
end)
---------------------------------------------------------
local page2 = venyx:addPage("TP's", 5012544693) --page 2
local sect1 = page2:addSection("TP's")

sect1:addButton("Forge", function()
    tpForge()
end)

sect1:addButton("Heart River", function()
	tpRiver()
end)

sect1:addButton("Upgrader", function()
	tpUpgrade()
end)

sect1:addButton("Trade Center", function()
	tradeCenter()
end)

sect1:addButton("Dark Orb Creator", function()
	darkOrb()
end)

sect1:addButton("Night Essence Creator", function()
	nightEssence()
end)

sect1:addButton("Useless Hole", function()
	uselessHole()
end)

sect1:addButton("Trader (Tree)", function()
	trader()
end)
---------------------------------------------------------
local page3 = venyx:addPage("Others", 5012544693) --page 3
local secy1 = page3:addSection("Others")

local relicSelected
secy1:addDropdown("Relics", {"None", "bday_relic", "dev relic", "mout_relic", "streetfighting_relic", "thevoices_relic"}, function(text)
	relicSelected = text
	print("Selected", text)
end)

secy1:addToggle("Relic Farm", function()
	while selectedWorld do
		relicFarm(relicSelected)
	end
end)
